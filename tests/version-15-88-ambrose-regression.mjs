import assert from "node:assert/strict";
import { readFileSync } from "node:fs";

const app = readFileSync(new URL("../app.js", import.meta.url), "utf8");
const css = readFileSync(new URL("../styles.css", import.meta.url), "utf8");
const html = readFileSync(new URL("../index.html", import.meta.url), "utf8");
const sw = readFileSync(new URL("../sw.js", import.meta.url), "utf8");

assert.match(html, /Version 15\.90\.7/);
assert.match(sw, /away-golf-v15-90-7/);
assert.match(app, /ambrose:\s*"Ambrose"/);
assert.match(app, /id:\s*"ambrose"/);
assert.match(app, /function renderAmbroseScoring/);
assert.match(app, /Drive taken\?/);
assert.match(app, /ambroseMinimumDrives/);
assert.match(app, /ambroseScoringMode/);
assert.match(app, /Required drives per player/);
assert.match(app, /const ambroseComp = [\s\S]*ambroseComp\.insertAdjacentHTML/);
assert.match(app, /ambroseBenefitRow/);
assert.match(app, /ambroseMinimumInline/);
assert.match(app, /\[2, 3, 4\]/);
assert.match(app, /Click to include a Score Marker/);
assert.match(app, /Applies to every team in this event/);
assert.match(app, /id="toggleAmbroseMarker"/);
assert.doesNotMatch(app, /Ambrose Team Roles/);
assert.match(app, /name="ambroseNtpCount" value="0"/);
assert.match(app, /name="ambroseNtpCount" value="1"/);
assert.match(app, /name="ambroseNtpCount" value="2"/);
assert.match(app, /Who signed the NTP card\?/);
assert.match(app, /confirmedAt: new Date\(\)\.toISOString\(\)/);
assert.match(app, /TWO SHOTS:/);
assert.match(app, /extraNtpPlayerId = roles\.team\.length === 3/);
assert.match(app, /setup\.ntpExtraPlayers = assignments/);
assert.match(app, /eligible\.length > 1 \? used : \[\]/);
assert.match(app, /index > 0 && used\.includes\(selected\)/);
assert.match(app, /The extra attempt rotates to a different golfer/);
assert.match(app, /function ambroseTeamHandicap/);
assert.match(app, /Provisional leader/);
assert.match(app, /rowComplete && r\.handicap != null/);
assert.match(app, /values\.reduce[\s\S]*team\.length \* 2/);
assert.match(app, /def\.type === "ambrose"/);
assert.match(css, /\.ambroseDriveChoices/);
assert.match(css, /\.ambroseFinalScore/);
assert.match(app, /Hole \$\{hole\} is incomplete/);
assert.match(app, /class="stepValue" id="ambroseGross"/);
assert.match(app, /targetPosition > position/);
assert.match(app, /Tap correct player to correct an error/);
assert.match(app, /REQUIRED DRIVES NOT RECORDED/);
assert.match(app, /ambroseDriveCompliant/);
assert.match(app, /minimum satisfied/);
assert.match(app, /ambroseThreePlayerContext/);
assert.match(app, /Final Net/);
assert.match(app, /is your marker/);
assert.match(app, /is your scorer/);
assert.match(app, /ambroseRoleAssignments/);
assert.match(app, /data-ambrosescorer/);
assert.match(app, /data-ambrosemarker/);
assert.match(app, /function ambroseTeamHasEntries/);

console.log("Version 15.88 Ambrose regression checks passed.");
